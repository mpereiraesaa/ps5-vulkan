#version 450
layout(local_size_x = 64) in;
layout(set = 0, binding = 0, std430) buffer Values { uint values[]; } data;
void main()
{
    uint i = gl_GlobalInvocationID.x;
    data.values[i] = data.values[i] * 3u + 7u;
}
